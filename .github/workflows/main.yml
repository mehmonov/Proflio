name: Django CI/CD

on:
  push:
    branches:
      - master  

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Install SSH Key
        uses: appleboy/ssh-action@master
        with:
          host: "77.232.128.142"
          username: "root"
          password: "v376f.AYmf@#Rx"

      - name: Deploy to Server
        run: |
          ssh -o StrictHostKeyChecking=no root@77.232.128.142 << 'EOF'
          cd /home/Proflio
          git pull
          source /home/venv/bin/activate
          pip install -r req.txt
          supervisorctl restart all
          EOF

      - name: Debug SSH connection
        env:
          HOST: 77.232.128.142
          USER: root
        run: |
          ssh -vvv -o StrictHostKeyChecking=accept-new $USER@$HOST echo "SSH connection successful"
